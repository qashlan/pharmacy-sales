================================================================================
  🔐 OPENAI API KEY SETUP - QUICK START
================================================================================

To enable OpenAI GPT features, follow these 3 simple steps:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Get an OpenAI API Key
────────────────────────────────────────────────────────────────────────────
  1. Visit: https://platform.openai.com/api-keys
  2. Sign up or log in
  3. Click "Create new secret key"
  4. Copy the key (starts with sk-...)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: Create .env File
────────────────────────────────────────────────────────────────────────────
  Option A - Copy the template:
    $ cp env.example .env

  Option B - Create manually:
    $ nano .env
    
  Then add this line:
    OPENAI_API_KEY=sk-your-actual-key-here

  Save and close!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: Test and Run
────────────────────────────────────────────────────────────────────────────
  Test the setup:
    $ python3 test_openai_features.py

  Run the dashboard:
    $ streamlit run dashboard.py

  Look for: ✨ GPT Enhanced (green badge)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

THAT'S IT! 🎉
────────────────────────────────────────────────────────────────────────────
The system will automatically load your API key from the .env file.

💰 Cost: ~$1-15/month (each query costs < $0.01)
🔒 Security: .env file is already in .gitignore (safe from Git)
📖 Detailed Guide: See SETUP_ENV.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IMPORTANT NOTES:
────────────────────────────────────────────────────────────────────────────
  ✅ OpenAI is OPTIONAL - system works fine without it
  ✅ .env file must be in project root directory
  ✅ Never commit .env to Git (it's already ignored)
  ✅ No spaces around the = sign in .env file

TROUBLESHOOTING:
────────────────────────────────────────────────────────────────────────────
  If you see "🔧 Pattern Matching" instead of "✨ GPT Enhanced":
  
  1. Check .env file exists: $ ls -la .env
  2. Check file contents: $ cat .env
  3. Verify API key format starts with: sk-
  4. Install dotenv: $ pip install python-dotenv
  5. See SETUP_ENV.md for detailed troubleshooting

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EXAMPLE .env FILE:
────────────────────────────────────────────────────────────────────────────
# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

================================================================================

